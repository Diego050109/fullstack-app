services:
  mongo:
    image: mongo:7
    container_name: mongo_fullstack
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: fullstackdb
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: